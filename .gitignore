.DS_Store
/.firebase
node_modules

# Log files
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.npm/
# firebase-tools/emulator debug logs — land wherever `firebase`/emulators:start
# is run from (repo root when running the Docker emulator recipe in
# CLAUDE.md, packages/functions for everything else)
firebase-debug.log
firestore-debug.log
ui-debug.log
database-debug.log
pubsub-debug.log
extensions-debug.log

# Editor directories and files
.idea
.vscode
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw*

packages/admin/dist
packages/admin/node_modules

packages/functions/lib
packages/functions/node_modules

packages/front/node_modules
packages/front/dist
packages/functions/conversation.txt
.env*
!.env.example
secrets

# Contains real Firebase project IDs — never commit, see .firebaserc.example
.firebaserc

# Contains emulator config — never commit, see .firebaserc.example
.config